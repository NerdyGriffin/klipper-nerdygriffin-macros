# Positioning Macros (positioning_macros.cfg)

Toolhead positioning helpers used by automated sequences and manual workflows.

## Usage

```gcode
CENTER    # Move toolhead to bed center
```

- `CENTER` — Homes if needed, then moves the toolhead to the center of the bed at maximum travel speed.
